feat: RAGForge Precision Ingestion™ & Edge VLM Upgrade

Core Pipeline Enhancements:
- Replaced `all-MiniLM-L6-v2` with `BAAI/bge-m3` for 1024-dim, 8192-token dense embeddings
- Rewrote `ragforge_indexer.py` to use `docling` for digital PDFs (retains tables, equations, multi-column layouts)
- Implemented Phase 2 Semantic Chunking honoring structural boundaries (sections, figures, equations)

Scanned & Complex Document Processing:
- Added `vlm_processor.py` (ported from Ventro) for academic visual extraction
- Added automated routing logic to send image-heavy or scanned PDFs to local VLM
- Set `llama3.2-vision` as default M1-optimized VLM engine

Hallucination Defense (SAMR-lite):
- Created `samr_lite.py` for in-memory semantic cosine similarity faithfulness checks
- Exempted RAGForge from strict Silicon Colosseum blocking in favor of transparent ⚠️ low-confidence user badges
- Implemented strict 7-rule RAGForge system prompt in `meta_agent.py` forbidding training-data usage
- Recalibrated `SILICON_COLOSSEUM_MIN_FAITHFULNESS` from 0.92 (heuristic) to 0.45 (real cosine)

Systems & Infrastructure:
- Added startup safety-net in `main.py` and `.env` routing HF_HOME, TRANSFORMERS_CACHE, and TORCH_HOME to external volume (`/Volumes/Apple/AI Model/hf_cache`) to protect iMac internal SSD space

Intent Engine Action Gap Fixes:
- Added execution context prompts for StreamSync, TuneLab, WatchTower to act rather than explain
